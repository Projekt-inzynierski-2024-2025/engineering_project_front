namespace engineering_project.Constants
{
    public static class Enums
    {
        public enum Role { admin, manager, employee }

        public enum AvailabilityType { full_day, from_to }
        public enum AvailabilityStatus { active, blocked }

        public enum WorkStatus { active, finished }

        public enum DailyScheduleStatus { open, closed }
    }
}
